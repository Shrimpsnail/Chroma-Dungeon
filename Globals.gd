extends Node

var musicProgress = 0.0 #By being at 0, it will always start at the beginning when you launch the game

var hasSaved = false
var counter = 0

var redPos = Vector2.ZERO
var greenPos = Vector2.ZERO
var bluePos = Vector2.ZERO

var redCPos = Vector2.ZERO
var greenCPos = Vector2.ZERO
var blueCPos = Vector2.ZERO
